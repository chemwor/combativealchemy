# Netlify configuration for this Angular project
# Build command uses the npm script defined in package.json ("build" -> "ng build").
# Publish directory matches the `outputPath` from angular.json ("./dist").

[build]
  command = "npm run build"
  publish = "dist"

# Ensure single-page-app routing works by redirecting all requests to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = true

# Optional: production environment variables (uncomment & set values if needed)
#[context.production.environment]
#  NODE_ENV = "production"
#  # Example: API_URL = "https://api.example.com"

